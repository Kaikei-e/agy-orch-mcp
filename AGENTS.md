# agy-first 運用方針

本プロジェクトおよび関連リポジトリでは、タスク実行の主力を Google Antigravity CLI（`agy`）とし、外部フロンティアモデルをオーケストレーターとする **agy-first** 体制を採用します。

## 役割分担 (Role Distinction)

- **外部フロンティアホスト (Frontier Orchestrator)**
  - 対象: Codex CLI / IDE や Claude Code などの外部ホスト環境。
  - 役割: 全体の方針決定、タスク分割（指示パケットの策定）、進捗管理、最終差分（diff）およびエビデンスのレビューに専念します。
  - 原則: 外部ホスト自身が広範なリポジトリ調査、Web 検索、コード編集やテスト実行を直接行うことは避け、`agy-orch-mcp` を通じて agy に委譲します。

- **agy ワーカー (Execution Engine)**
  - 対象: `agy-orch-mcp` 経由で起動される Google Antigravity CLI (`agy`)。
  - 役割: リポジトリ調査、Web 仕様調査、実装、テスト作成・実行、自己修正（セルフテストと修正ループ）のすべてを担当します。
  - **重要（再帰・逆委譲の禁止）**: 委譲を受けた agy ワーカー自身は、提供されているネイティブツールを用いて自律的にタスクを完了します。自身から `agy-orch-mcp` を再帰的に呼び出したり、外部ホストへ作業を差し戻して（逆委譲して）はなりません。

## 指示パケット (Task Packet) の仕様

外部ホストが agy へタスクを委譲する際は、曖昧さを排除した以下の構造化パケットを作成して渡します。

1. **ゴール (Goal)**: 達成すべき目的と作業範囲を明確に定義する。
2. **スコープとファイルの所有権 (Scope & File Ownership)**:
   - 変更を許可する対象ファイル・ディレクトリを明示する。
   - 変更を禁止するファイル（他のワーカーの担当範囲や設定ファイルなど）を明示し、並列作業時のファイル競合を防止する。
3. **制約条件・受入基準・テスト計画 (Constraints, Acceptance Criteria, Test Plan)**:
   - コーディング規約、安全要件、互換性、テストコマンド（例: `pnpm test`）を明示する。
4. **返却すべきコンパクトなエビデンス (Compact Evidence)**:
   - agy 完了時に報告すべき内容（変更したファイルパス一覧、実行した検証コマンドと結果、参照した外部 URL と取得した要点、残存課題やブロッカー）を指定する。

## agy 実行と並列・継続運用

- **セルフテストと修正ループ**:
  - agy 内で実装・テスト・検証・修正まで自律的に完結させます。外部ホストは最終結果の確認のみを行います。
- **会話の継続 (Continue)**:
  - `antigravity_continue` は同じ会話の直接の続き（直前のタスクへの追加修正・追質問）にのみ使い、`antigravity_run` が返した `conversation_id` を必ず指定します。
  - 別の独立したタスクは、既存会話の流用ではなく新しい `antigravity_run` で開始します。
- **並列化の使い分け (タスク種別で選ぶ)**:
  - **コード変更を伴う並列タスク**: `antigravity_batch` を使います。各タスクが独立した git worktree で実行され、`owns` の検証を経て `final.patch` として返るため、ワークスペースの競合が起きません。ホストのツール並列実行仕様にも依存しません（1 回のツール呼び出し内でサーバーが並列実行します）。実行前にワークスペースがクリーン（未コミット変更がない状態）であることを確認してください。サーバー上限を超える budget 値は自動クランプされ、進捗通知（ハートビート）により長時間バッチでも切断されません。
  - **単発タスク・同一会話の続き**: `antigravity_run` → ID 明示の `antigravity_continue`。
  - **読み取り専用の調査の並列化**: 異なる会話の `antigravity_run` を同時に発行します（サーバー既定で最大 4 件同時実行）。ただし同時発行が実際に並列実行されるかはホストに依存します（下記）。
- **ホスト別の並列実行仕様**:
  - `antigravity_run` / `antigravity_continue` は `readOnlyHint: false` のため、ホストは既定で 1 件ずつ実行します。
  - **Claude Code**: 同一メッセージ内の同時発行でも直列実行されます。並列化したい場合は `antigravity_batch` を使うか、サブエージェントごとに `antigravity_run` を 1 件ずつ発行します。
  - **Codex CLI / IDE**: MCP サーバー設定に `supports_parallel_tool_calls = true` を指定すると並列実行されます。同一会話への重複呼び出しはサーバー側ロックが `BUSY` で拒否します。
- **モデルの選定とキャッシュ**:
  - 利用可能なモデル slug は事前に `antigravity_models` で確認・キャッシュして使用するか、サーバー環境変数 `AGY_MCP_DEFAULT_MODEL` / CLI 既定値を利用し、モデル選定のための無駄なターン消費を抑えます。

## 境界のあるエラー復旧 (Bounded Recovery)

- **`BUSY` 応答時**:
  - 同時実行上限超過や排他制御による一時的ビジーです。リトライストーム（短い間隔での連続再試行）を避け、待機するか直列化して呼び出します。
- **タイムアウトまたは空応答 (`EMPTY_RESPONSE`) 時**:
  - プロセスが中断された場合でも作業ディレクトリに変更（副作用）が生じている可能性があるため、再試行前に必ず `git status` やファイル差分を確認します。
- **権限拒否 (`PERMISSION_DENIED`) やエラー時**:
  - agy 側で権限拒否（denied actions）やエラーが発生した場合、暗黙的に外部ホストへ作業を戻してホスト自身が代行してはなりません。具体的なブロッカー（不足している権限フラグや設定）を明確にユーザーへ報告します。

# 個人用 MCP クライアント設定

- agy をプロジェクトへ導入するときは、作成する個人環境に依存した `.mcp.json`、`.codex/`、`.claude/settings.local.json` を Git 管理しない。対象リポジトリの `.gitignore` に必要な明示ルールを追加し、`git check-ignore -v` と `git status --short` で除外と意図しない変更がないことを確認する。
