# Merge this example into ~/.codex/config.toml, or into .codex/config.toml
# in a trusted project. Replace every placeholder with an absolute path.
# Preserve existing settings and edit an existing table with this name rather
# than defining [mcp_servers.antigravity] twice.
# After updating configuration, restart the Codex CLI or IDE session.

[mcp_servers.antigravity]
command = "/absolute/path/to/node"
args = ["/absolute/path/to/agy-orch-mcp/dist/index.js"]
startup_timeout_sec = 20
tool_timeout_sec = 3660
# Lets Codex run simultaneous antigravity_run calls concurrently; the server
# still rejects same-conversation overlaps and calls above the limit with BUSY.
supports_parallel_tool_calls = true

[mcp_servers.antigravity.env]
AGY_MCP_DEFAULT_WORKSPACE = "/absolute/path/to/workspace"
AGY_MCP_ALLOWED_ROOT = "/absolute/path/to"
AGY_MCP_MAX_CONCURRENT = "4"
# Recommended compact output limit (characters) to conserve host context window:
# AGY_MCP_MAX_OUTPUT_CHARS = "16000"
# Optional default model slug discovered via `antigravity_models`:
# AGY_MCP_DEFAULT_MODEL = "a-slug-returned-by-antigravity_models"
# Remove this entry when `agy` is available in Codex's PATH:
AGY_MCP_BIN = "/absolute/path/to/agy"
